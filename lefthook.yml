pre-commit:
    parallel: true
    jobs:
        - name: oxlint
          glob: '*.{js,ts,jsx,tsx,astro,mjs}'
          run: pnpm lint
        - name: oxfmt
          glob: '*.{js,ts,jsx,tsx,astro,mjs,json,css}'
          run: pnpm fmt:check
